# GitHub Creation Instructions

If the remote repository has not been created yet, follow these steps.

## Intended Repository
- Name: `memory-poisoning-lab-notes`
- Suggested visibility: `Public`

## Recommended Remote URL Pattern
`https://github.com/<your-username>/memory-poisoning-lab-notes.git`

## First Push Steps
From this repository root:

```bash
git remote add origin https://github.com/<your-username>/memory-poisoning-lab-notes.git
git branch -M main
git push -u origin main
```

After creating this repo, update the README links in both repositories with final URLs.
